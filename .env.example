

POSTGRES_DB=drive
POSTGRES_USER=drive
POSTGRES_PASSWORD=drivepass

# Local MinIO development credentials. Choose your own password in .env and
# never reuse these credentials in production.
MINIO_ROOT_USER=drive_minio
MINIO_ROOT_PASSWORD=change-this-local-password
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET=drive-files
